{
  "name": "git-me",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "font-awesome": "^4.7.0",
    "lunr": "^2.1.4",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-router": "^4.2.0",
    "react-scripts": "1.0.14",
    "rimraf": "^2.6.2"
  },
  "scripts": {
    "cleanIndex": "rimraf public/documents.json public/documents-idx.json",
    "createIndex": "node build/build-index resources/git-cheat-sheet.json public documents",
    "resetIndex" : "npm run cleanIndex && npm run createIndex", 
    "start": "npm run resetIndex && react-scripts start",
    "build": "npm run resetIndex && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
